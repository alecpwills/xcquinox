name: test
channels:

  - conda-forge

  - defaults
dependencies:
    # Base depends
  - python
  - pip:
    - pyscfad
    - mldftdat @ git+https://github.com/alecpwills/xcquinox-cider@main
    - pypatch
    # Testing
  - pytest
  - pytest-cov
  - codecov
  - jax[cpu]
  - equinox
  - ase
  - optax
  - psutil
