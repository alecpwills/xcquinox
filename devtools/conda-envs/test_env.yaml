name: test
channels:

  - conda-forge

  - defaults
dependencies:
    # Base depends
  - python
  - pip:
    - pyscfad

    # Testing
  - pytest
  - pytest-cov
  - codecov
  - jax[cpu]
  - equinox
  - ase
