This notebook trains 72 neural network exchange-correlation functionals (12 architectures × 6 training approaches) for DFT, using a hybrid method that combines fixed-density energy evaluation on PBE densities with a fast pure-JAX one-shot density prediction algorithm (~100× faster than pyscfad) to match CCSD density matrices and grid densities while targeting literature atomization energies for H/O/H₂O; the architectures range from simple MLPs to deep networks with self-attention and physics-informed features (nuclear cusp proximity, density matrix correlation indicators), and the training approaches compare energy-only learning (A, D1) against density-matching variants (B, C, D2, D3) to investigate whether matching CCSD densities improves energy predictions and whether the networks can achieve chemical accuracy.